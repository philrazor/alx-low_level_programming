#include <stdio.h>
#include "main.h"
/**
 * mul - takes two integers and multiplies them
 * @a: is the first integer
 * @b: is the second integer
 * Return: 0 always
 */
int  mul(int a, int b)
{
	int c = a * b;

	return (c);
}
